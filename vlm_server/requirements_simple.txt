# Requirements for Simple Screen Capture VLM Control
# Complete dependencies for autonomous Duckiebot control via VLM

# Computer Vision and Image Processing
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# HTTP Communication with VLM Server
requests>=2.30.0

# Web Dashboard
Flask>=2.3.0

# Screen Capture and Window Management
mss>=9.0.0
pygetwindow>=0.0.9

# Keyboard and Mouse Automation
pyautogui>=0.9.50
keyboard>=0.13.5

# Additional Windows-specific dependencies (for better compatibility)
pywin32>=306; platform_system=="Windows"

# GUI Libraries (usually included with Python, but just in case)
# tkinter is built-in, but some minimal Python installations might not have it

# JSON, argparse, time, threading, base64 are built-in Python modules
